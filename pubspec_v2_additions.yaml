dependencies:
  # Existing dependencies remain...
  
  # NEW V2 Dependencies for Clean Architecture
  # State Management
  flutter_riverpod: ^2.5.1
  riverpod_annotation: ^2.3.5
  
  # Navigation
  go_router: ^14.0.0  # Updated from 12.1.1
  
  # Network
  retrofit: ^4.0.3
  dio: ^5.4.0  # Keep existing
  pretty_dio_logger: ^1.3.1
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Functional Programming
  dartz: ^0.10.1
  freezed_annotation: ^2.4.1
  
  # AI Integration
  google_generative_ai: ^0.2.2  # Gemini API
  
  # Utils
  intl: ^0.18.1  # Keep existing
  cached_network_image: ^3.3.1  # Keep existing
  
  # UI/UX
  flutter_animate: ^4.5.0
  shimmer: ^3.0.0
  lottie: ^3.0.0
  
dev_dependencies:
  # Build runners
  build_runner: ^2.4.8
  freezed: ^2.4.7
  json_serializable: ^6.7.1
  riverpod_generator: ^2.3.11
  hive_generator: ^2.0.1
  retrofit_generator: ^8.0.6
  
  # Testing
  mocktail: ^1.0.0  # Keep existing
  flutter_test:
    sdk: flutter
